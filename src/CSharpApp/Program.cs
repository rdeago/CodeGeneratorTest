using System;

namespace CSharpApp
{
    internal static class Program
    {
        // Proof that even a C# 7.3 project can use code generators.
        static void Main() => Console.WriteLine(Hello.World);
    }
}
